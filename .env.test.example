# E2E Test Environment Configuration
# Copy this file to .env.test and fill in your New Relic credentials

# Primary Test Account (Required)
# This should be an account with diverse data types and reasonable volume
NEW_RELIC_API_KEY_PRIMARY=your-primary-api-key-here
NEW_RELIC_ACCOUNT_ID_PRIMARY=your-primary-account-id
NEW_RELIC_REGION_PRIMARY=US  # or EU

# Secondary Test Account (Optional but recommended)
# This should have different naming conventions or schema patterns
NEW_RELIC_API_KEY_SECONDARY=your-secondary-api-key-here
NEW_RELIC_ACCOUNT_ID_SECONDARY=your-secondary-account-id
NEW_RELIC_REGION_SECONDARY=US

# Empty Test Account (Optional)
# This should have no active data ingestion for zero-data scenario testing
NEW_RELIC_API_KEY_EMPTY=your-empty-api-key-here
NEW_RELIC_ACCOUNT_ID_EMPTY=your-empty-account-id
NEW_RELIC_REGION_EMPTY=US

# High Cardinality Test Account (Optional)
# This should have 1000+ services and high event volume for scale testing
NEW_RELIC_API_KEY_HIGH_CARD=your-high-card-api-key-here
NEW_RELIC_ACCOUNT_ID_HIGH_CARD=your-high-card-account-id
NEW_RELIC_REGION_HIGH_CARD=US

# Test Execution Configuration
E2E_TEST_TIMEOUT=300s           # Maximum time for entire test suite
E2E_PARALLEL_TESTS=4            # Number of parallel test executions
E2E_RETRY_ATTEMPTS=3            # Retry count for flaky tests
E2E_CACHE_DISABLED=true         # Disable caching during tests for fresh data

# Performance Test Thresholds
E2E_PERF_DISCOVERY_TIMEOUT=5s   # Max time for discovery operations
E2E_PERF_QUERY_TIMEOUT=10s      # Max time for query operations
E2E_PERF_WORKFLOW_TIMEOUT=300s  # Max time for complete workflows
E2E_PERF_MEMORY_LIMIT=500MB     # Max memory usage

# Debug Configuration
E2E_LOG_LEVEL=info              # Log level: debug, info, warn, error
E2E_CAPTURE_TRAFFIC=false       # Capture MCP protocol traffic
E2E_SAVE_RESPONSES=false        # Save all API responses for debugging

# Data Generation (Optional)
E2E_GENERATE_TEST_DATA=false    # Generate test data before running tests
E2E_TEST_DATA_PREFIX=e2e-test   # Prefix for generated test data
E2E_CLEANUP_TEST_DATA=true      # Clean up test data after tests

# Regional Testing (Optional)
E2E_TEST_REGIONS=US,EU          # Comma-separated list of regions to test
E2E_CROSS_REGION_TESTS=false    # Enable cross-region correlation tests

# Chaos Testing (Optional)
E2E_CHAOS_ENABLED=false         # Enable chaos/fault injection tests
E2E_CHAOS_NETWORK_DELAY=0ms     # Artificial network delay
E2E_CHAOS_ERROR_RATE=0          # Artificial error rate (0-100)